[pytest]
# Standalone pytest config for nrdr_radar_re — prevents the worktree root conftest
# (which requires on-device openpilot modules) from being loaded when running this
# tool's tests in isolation on Windows.
testpaths = tests
