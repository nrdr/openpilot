"""NRDR-owned on-road UI components.

The package intentionally has no eager imports so metadata and prebuilt tools
can import :mod:`openpilot.nrdr.ui` without initializing pyray or the UI runtime.
"""
